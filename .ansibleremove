# Relative and ~/ paths resolve beneath the managed user's home; absolute paths are used verbatim.
# Files, symlinks, and directories are removed recursively when present.
.config/zsh/tmux-theme.zsh
.pi/agent/claude-bridge.json

# Codex disables orphan cleanup, so retired skills must be declared here.
.codex/skills/launch-video
.codex/skills/bro
.codex/skills/writing-great-skills
