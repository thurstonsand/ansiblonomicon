# Relative and ~/ paths resolve beneath the managed user's home; absolute paths
# are used verbatim. Files, symlinks, and directories are removed recursively
# when present. This is .ansibleremove, consumed by //units/base:retire.

# Trial demonstration: a script this segment used to deploy.
/usr/local/bin/retired-storage-probe
.cache/retired-alerting-scratch
