# Auto-activate pre-commit hooks when entering directory
if command -v pre-commit &> /dev/null; then
  if [ ! -f .git/hooks/pre-commit ]; then
    log_status "Installing pre-commit hooks..."
    pre-commit install
  fi
fi
