---
- name: Render global Amp skills
  hosts: localhost
  connection: local
  gather_facts: true

  vars_files:
    - ../config.yml
    - ../agent-harness.config.yml
    - ../models.yml

  roles:
    - role: agent_harness
      agent_harness_profile: amp_publish
