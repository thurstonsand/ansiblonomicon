[defaults]
# Inventory file location
inventory = inventory

# Roles search path
roles_path = ./roles

# Default to using sudo/become
become = true

# Use YAML output format (easier to read)
callback_result_format = yaml

# Disable cowsay (if installed)
nocows = true

[ssh_connection]
# Speeds up execution by reusing SSH connections
pipelining = true
