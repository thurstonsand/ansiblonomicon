---
- name: Ensure sshd config drop-in directory exists
  become: true
  ansible.builtin.file:
    path: /etc/ssh/sshd_config.d
    state: directory
    mode: "0755"

- name: Configure managed sshd settings
  become: true
  ansible.builtin.template:
    src: 99-ansiblonomicon.conf.j2
    dest: /etc/ssh/sshd_config.d/99-ansiblonomicon.conf
    mode: "0644"
  notify: Validate and reload sshd
