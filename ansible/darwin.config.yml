---
# macOS-specific configuration

# Feature flags
configure_homebrew: true
configure_macos_defaults: true

# Homebrew packages
# These will be populated from homebrew.nix migration
homebrew_taps: []
homebrew_cask_apps: []
homebrew_installed_packages: []
mas_installed_apps: []
