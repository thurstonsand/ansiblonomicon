#!/usr/bin/env bash
set -euo pipefail

# The enter hook never fires here, so trust and bootstrap are invoked directly.
mise trust --quiet

if [[ -n "${OP_SERVICE_ACCOUNT_TOKEN:-}" ]]; then
  mise run bootstrap
fi
